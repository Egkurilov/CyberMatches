# CyberMatches 🎮

Автоматизированная система мониторинга киберспортивных матчей Dota 2 с парсингом данных из Liquipedia, REST API и Telegram-ботом для уведомлений.

## 📋 Описание

CyberMatches — это комплексное решение для отслеживания матчей Dota 2, которое:
- Автоматически парсит расписание матчей с Liquipedia
- Хранит данные в PostgreSQL с умным определением статусов
- Предоставляет REST API для получения данных
- Отправляет уведомления через Telegram-бота с гибкими фильтрами

## ✨ Основные возможности

### 🔍 Парсер матчей (main.py)
- ⏱️ Автоматический сбор данных каждые 10 минут
- 🌍 Поддержка множества часовых поясов (UTC, CET, MSK, PST, EDT и др.)
- 📊 Определение статусов: `upcoming`, `live`, `finished`
- 🎯 Автоматическое обновление счетов из детальных страниц матчей
- 📝 JSON-логирование всех событий
- 🔄 Обработка вкладок "Upcoming" и "Completed"

### 🌐 REST API (api.py)
```
GET /dota/matches/today          # Матчи на сегодня (МСК)
GET /dota/matches/{dd-mm-yyyy}   # Матчи на конкретную дату
```

### 🤖 Telegram-бот (cyber_telegram_bot.py)
- 📅 Команда `/today` - просмотр матчей с автообновлением
- 🔔 Ежедневные уведомления в 10:00 МСК
- 🎚️ Фильтрация по турнирам через inline-кнопки
- 📱 Одно обновляемое сообщение на день
- 🟢 Группировка: LIVE → Скоро → Завершённые

### 🛠️ Утилиты
- `today_matches.py` - CLI для просмотра сегодняшних матчей
- `fix_tournaments.py` - Очистка названий турниров
- `cleanup_duplicates.py` - Удаление дубликатов
- `ref.py` - Пересчёт уникальных идентификаторов

## 🏗️ Архитектура

```
┌─────────────────┐
│   Liquipedia    │
└────────┬────────┘
         │ Парсинг
         ▼
┌─────────────────┐      ┌──────────────┐
│    main.py      │─────▶│ PostgreSQL   │
│   (Парсер)      │      │   Database   │
└─────────────────┘      └──────┬───────┘
                                │
                    ┌───────────┴───────────┐
                    │                       │
              ┌─────▼──────┐        ┌──────▼─────┐
              │   api.py   │        │cyber_telegram│
              │ (FastAPI)  │        │   _bot.py   │
              └────────────┘        └──────┬──────┘
                                           │
                                    ┌──────▼──────┐
                                    │  Telegram   │
                                    │    Users    │
                                    └─────────────┘
```

## 🗄️ База данных

### Таблица `dota_matches`
```sql
- id (SERIAL PRIMARY KEY)
- match_time_msk (TIMESTAMPTZ) - время матча (МСК)
- match_time_raw (TEXT) - оригинальное время
- team1 (TEXT) - первая команда
- team2 (TEXT) - вторая команда
- score (TEXT) - счёт, например "2:1 Bo3"
- bo (INT) - формат (Bo1, Bo3, Bo5)
- tournament (TEXT) - название турнира
- status (TEXT) - upcoming/live/finished
- match_uid (TEXT UNIQUE) - уникальный идентификатор
- match_url (TEXT) - ссылка на страницу матча
- created_at, updated_at (TIMESTAMPTZ)
```

### Таблицы бота
- `dota_bot_subscribers` - подписчики
- `dota_bot_today_messages` - состояния сообщений /today

## 🚀 Установка и запуск

### Требования
- Python 3.9+
- PostgreSQL 12+
- Telegram Bot Token

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка окружения

Создайте файл `.env`:

```env
# База данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cybermatches
DB_USER=postgres
DB_PASSWORD=your_password

# Парсер
SCRAPE_INTERVAL_SECONDS=600  # 10 минут

# Telegram бот
TELEGRAM_BOT_TOKEN=your_bot_token
MATCHES_API_URL=http://localhost:8050/dota/matches/today
POLL_INTERVAL_SECONDS=60
```

### 3. Инициализация базы данных

```bash
# Создайте БД и таблицы (SQL скрипт будет создан автоматически при первом запуске)
```

### 4. Запуск компонентов

**Парсер:**
```bash
python main.py
```

**REST API:**
```bash
uvicorn api:app --host 0.0.0.0 --port 8050
```

**Telegram бот:**
```bash
python cyber_telegram_bot.py
```

## 📖 Использование

### CLI - Просмотр матчей

```bash
python today_matches.py
```

Вывод:
```
Матчи на сегодня (МСК) — 2025-11-30
--------------------------------------------------------------------------------
18:00 MSK  | Tundra vs Liquid          (Bo3) | DreamLeague S25 | ⏰ upcoming
20:30 MSK  | Team Spirit vs OG         (Bo5) | ESL One Berlin  | 🟢 live | 1:1
```

### REST API

**Получить матчи на сегодня:**
```bash
curl http://localhost:8050/dota/matches/today
```

**Получить матчи на конкретную дату:**
```bash
curl http://localhost:8050/dota/matches/26-11-2025
```

Пример ответа:
```json
{
  "date": "2025-11-26",
  "timezone": "Europe/Moscow",
  "matches": [
    {
      "match_time_msk": "2025-11-26T18:00:00+03:00",
      "time_msk": "18:00",
      "team1": "Tundra",
      "team2": "Liquid",
      "bo": 3,
      "tournament": "DreamLeague S25",
      "status": "upcoming",
      "score": null
    }
  ]
}
```

### Telegram бот

1. Найдите бота в Telegram
2. Отправьте `/start` для подписки на уведомления
3. Используйте `/today` для просмотра матчей с автообновлением
4. Кликайте на кнопки турниров для фильтрации

## 🔧 Утилиты обслуживания

**Очистка названий турниров:**
```bash
python fix_tournaments.py
```

**Удаление дубликатов:**
```bash
python cleanup_duplicates.py
```

**Пересчёт match_uid:**
```bash
python ref.py
```

## 📁 Структура проекта

```
CyberMatches/
├── main.py                    # Парсер матчей (основной движок)
├── api.py                     # REST API (FastAPI)
├── cyber_telegram_bot.py      # Telegram бот
├── today_matches.py           # CLI для просмотра матчей
├── fix_tournaments.py         # Утилита очистки турниров
├── cleanup_duplicates.py      # Утилита удаления дубликатов
├── ref.py                     # Утилита пересчёта UID
├── requirements.txt           # Зависимости Python
├── .env                       # Конфигурация (не в git)
├── logs/                      # Логи
│   ├── parser.log            # Логи парсера
│   └── bot.log               # Логи бота
└── data/                      # Данные (опционально)
```

## 🛠️ Технологии

**Backend:**
- **Python 3.9+** - основной язык
- **psycopg** - PostgreSQL драйвер (нативный async)
- **requests** + **BeautifulSoup** + **lxml** - веб-парсинг
- **FastAPI** - современный веб-фреймворк
- **aiogram** - асинхронный Telegram bot framework
- **python-dotenv** - управление конфигурацией

**База данных:**
- **PostgreSQL 12+** - хранение данных

**Инфраструктура:**
- JSON-логирование для мониторинга
- Автоматическое определение статусов
- Дедупликация через match_uid

## 🔄 Алгоритм работы

### Определение статуса матча

```python
if есть_финальный_счёт:
    status = "finished"
elif текущее_время < время_матча - 5мин:
    status = "upcoming"
elif текущее_время > предполагаемое_время_окончания + 15мин:
    status = "finished"
else:
    status = "live"
```

### Обновление счетов

1. Проверка детальной страницы матча (если есть URL)
2. Парсинг вкладки "Completed" на главной странице
3. Fallback-стратегии для надёжности

### Уникальность матчей

```python
match_uid = f"{team1_lower}|{team2_lower}|{tournament_lower}|{bo_number}"
```

## 📊 Логирование

Все события логируются в JSON формате:

```json
{
  "time": "2025-11-30 14:20:00",
  "event": "parse_iteration",
  "level": "INFO",
  "message": "Распарсили матчей: 42",
  "scope": "worker_once",
  "total": 42,
  "upcoming": 25,
  "live": 5,
  "finished": 12
}
```

## ⚙️ Конфигурация

### Интервалы обновления

- **Парсер**: 600 сек (10 мин) - `SCRAPE_INTERVAL_SECONDS`
- **Бот (поллинг)**: 60 сек - `POLL_INTERVAL_SECONDS`
- **Ежедневные уведомления**: 10:00 МСК

### Поддерживаемые часовые пояса

UTC, GMT, CET, CEST, EET, EEST, MSK, SGT, PST, PDT, EST, EDT, BST

Все времена конвертируются в МСК (UTC+3) для единообразия.

## 🤝 Разработка

### Добавление нового источника данных

1. Создайте функцию парсинга в `main.py`
2. Добавьте вызов в `worker_once()`
3. Обновите схему БД при необходимости

### Расширение API

1. Добавьте endpoint в `api.py`
2. Используйте `get_db_connection()` для работы с БД
3. Возвращайте данные в формате JSON

### Добавление команды в бота

1. Создайте handler в `cyber_telegram_bot.py`
2. Используйте декоратор `@dp.message(Command("название"))`
3. Обновите `/start` с информацией о новой команде

## 📝 Примечания

- Проект парсит публичные данные Liquipedia в соответствии с их ToS
- Используйте разумные интервалы опроса (не менее 60 сек)
- Рекомендуется настроить мониторинг логов для production
- Для масштабирования рассмотрите использование очереди задач (Celery)

## 📄 Лицензия

Проект разработан для личного использования. При использовании соблюдайте условия использования Liquipedia.

## 📧 Контакты

При возникновении вопросов или предложений создавайте Issues в репозитории проекта.

---

Создано с ❤️ для киберспортивного коммьюнити
