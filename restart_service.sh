#!/bin/bash
# Финальный перезапуск сервиса с новым кодом

echo "🔄 Перезапускаем сервис с исправленным кодом..."

# Останавливаем сервис
sudo systemctl stop cybermatches.service

# Ждем немного
sleep 2

# Запускаем сервис
sudo systemctl start cybermatches.service

# Проверяем статус
echo "📊 Статус сервиса:"
sudo systemctl status cybermatches.service

# Ждем немного для запуска
sleep 3

# Проверяем логи
echo "📋 Последние логи:"
sudo journalctl -u cybermatches.service -n 30 --no-pager

echo "✅ Перезапуск завершен!"
